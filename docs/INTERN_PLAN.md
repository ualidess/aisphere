# AI Sphere Avatar — план доработки до MVP (для стажёров)

## Цель

Превратить текущий прототип (голосовой вопрос-ответ через 3D-сферу) в **минимальный рабочий продукт**:

- голосовое общение с **ChatGPT** через сферу;
- **STT и TTS через ElevenLabs** (Whisper и OpenAI TTS убираем);
- **пользователи** со своей историей;
- **несколько чатов на одного пользователя** с возможностью переключения;
- **память диалога** (бот помнит контекст в рамках чата);
- **асинхронный** бэкенд;
- **логирование** и хранение истории в **БД**;
- **защита от нагрузок и взлома** (аутентификация, rate limiting, валидация).

Внешний API `policy_router_api` и связанную с ним external-сеть **полностью убираем**.

---

## Целевая архитектура (общая картина)

```
Браузер (Three.js сфера + экран входа + список чатов)
   │  HTTPS, JWT в заголовке
   ▼
Async backend (FastAPI)
   ├── Auth        (регистрация / вход / JWT)
   ├── Chats       (создать / список / переключить / переименовать / удалить)
   ├── Messages    (история сообщений чата)
   ├── /stt   → ElevenLabs Speech-to-Text (Scribe)
   ├── /chat  → OpenAI Chat Completions (с историей чата)
   └── /tts   → ElevenLabs Text-to-Speech
   │
   ▼
PostgreSQL (users, chats, messages)
```

### Рекомендуемый стек (решено за стажёров — не менять без согласования)

- **Backend:** FastAPI + Uvicorn (async-нативный, вместо синхронного Flask).
- **БД:** PostgreSQL. Доступ — async (SQLAlchemy 2.0 async + asyncpg). Миграции — Alembic.
- **Аутентификация:** JWT (access-токен), пароли — хэш (bcrypt/argon2).
- **HTTP к внешним API:** async-клиент (httpx).
- **Rate limiting:** slowapi (или аналог).
- **Логи:** структурированные (JSON), с request-id.
- **Внешние сервисы:** OpenAI (чат), ElevenLabs (STT + TTS).

---

## Порядок фаз и зависимости

Фазы 0→7 идут в основном последовательно. Внутри фаз часть задач параллелится.
Фронтенд (Фаза 8) можно вести параллельно бэкенду, начиная с Фазы 4.

---

## Фаза 0 — Подготовка и гигиена репозитория

> Цель: чистый старт, безопасные секреты.

- [x] Инициализировать git-репозиторий (сейчас не репозиторий) и сделать первый коммит.
- [x] Добавить `.gitignore` (Python, `.env`, `__pycache__`, виртуальные окружения, `*.mp3`/временные файлы).
- [x] **Убрать секреты из кода:** захардкоженный ключ внешнего API в `backend/app.py` и любые ключи из `.env` не должны попадать в репозиторий.
- [x] Создать `.env.example` со списком переменных без значений: `OPENAI_API_KEY`, `ELEVENLABS_API_KEY`, `ELEVENLABS_VOICE_ID`, `DATABASE_URL`, `JWT_SECRET`, и т.д.
- [ ] Получить и проверить ключи OpenAI и ElevenLabs (тестовый запрос к каждому API).

**Критерий приёмки:** репозиторий без секретов, `.env.example` актуален, ключи работают.

---

## Фаза 1 — Перевод бэкенда на async + прямой вызов ChatGPT

> Цель: заменить внешний API на OpenAI и перейти на асинхронный фреймворк.

- [x] Поднять каркас FastAPI вместо Flask (один рабочий эндпоинт + healthcheck `/`).
- [x] Реализовать `/chat`: запрос идёт напрямую в OpenAI Chat Completions (модель согласовать — по умолчанию недорогая, напр. `gpt-4o-mini`).
- [x] Добавить системный промпт (роль ассистента) — вынести в конфиг.
- [x] Все обращения к внешним API — асинхронные (httpx, без блокирующих вызовов).
- [x] Убрать из кода и из `docker-compose.yml` всё, что связано с `policy_router_api` и external-сетью `existing_api_network`.
- [x] Обновить Dockerfile бэкенда под FastAPI/Uvicorn.

**Критерий приёмки:** `/chat` отвечает реальным ответом ChatGPT; внешний API и его сеть в проекте больше не используются; сервер использует async-вызовы через `httpx.AsyncClient`.

---

## Фаза 2 — База данных и модели

> Цель: персистентное хранилище для пользователей, чатов и сообщений.

- [x] Добавить сервис PostgreSQL в `docker-compose.yml` (с volume для данных и healthcheck).
- [x] Спроектировать схему:
  - `users` (id, email/логин, хэш пароля, дата создания);
  - `chats` (id, user_id → users, title, дата создания/обновления);
  - `messages` (id, chat_id → chats, role [user/assistant/system], content, дата).
- [x] Настроить async-доступ к БД (SQLAlchemy async + asyncpg) и пул соединений.
- [x] Настроить миграции (Alembic), создать первую миграцию.
- [x] Сделать слой репозиториев/сервисов (CRUD), без бизнес-логики в обработчиках.

**Критерий приёмки:** миграции применяются на чистой БД; данные пишутся и читаются асинхронно.

---

## Фаза 3 — Пользователи и аутентификация

> Цель: у каждого свои чаты, эндпоинты защищены.

- [x] Эндпоинты `POST /auth/register` и `POST /auth/login`.
- [x] Хэширование паролей (bcrypt/argon2), без хранения паролей в открытом виде.
- [x] Выдача JWT при входе; проверка токена на защищённых эндпоинтах (dependency).
- [x] Все эндпоинты чатов/сообщений/STT/TTS требуют валидный токен и работают только с данными текущего пользователя.

**Критерий приёмки:** без токена защищённые ручки отдают 401; пользователь видит только свои чаты.

---

## Фаза 4 — Чаты, история и память диалога

> Цель: несколько чатов на пользователя + бот помнит контекст.

- [x] CRUD чатов: `POST /chats` (создать), `GET /chats` (список), `GET /chats/{id}` (сообщения), `PATCH /chats/{id}` (переименовать), `DELETE /chats/{id}`.
- [x] При запросе к `/chat` сообщение привязывается к конкретному `chat_id`.
- [x] Перед вызовом ChatGPT подгружать историю сообщений чата и передавать её в запрос (память диалога).
- [x] Сохранять в БД и вопрос пользователя, и ответ ассистента.
- [x] Ограничить длину передаваемой истории (последние N сообщений или по токенам), чтобы не раздувать запрос.
- [x] Авто-название чата по первому сообщению (опционально, по согласованию).
- [x] **Хранение 3 суток (72 ч):** чаты и сообщения старше 72 ч от последней активности удаляются автоматически.
  - Фоновая задача-уборщик (периодический запуск, напр. раз в час) удаляет `chats` с `updated_at` старше 72 ч, каскадно с их `messages`.
  - Срок считать от последней активности (`updated_at` обновляется при каждом новом сообщении), а не от создания чата.
  - На уровне БД: индекс по `updated_at` и каскадное удаление сообщений (`ON DELETE CASCADE`).
  - Фронтенд: показать пользователю, что чаты живут 72 часа.

**Критерий приёмки:** у пользователя несколько чатов, переключение сохраняет контекст; бот учитывает предыдущие реплики внутри чата; чаты старше 72 ч автоматически исчезают вместе с сообщениями.

---

## Фаза 5 — Переход на ElevenLabs (STT и TTS)

> Цель: и распознавание, и озвучка — через ElevenLabs.

- [x] `/stt`: распознавание речи через ElevenLabs Speech-to-Text (Scribe) вместо Whisper.
- [x] `/tts`: синтез речи через ElevenLabs Text-to-Speech; `voice_id` и модель — в конфиге/переменных окружения.
- [x] Убрать использование OpenAI audio (Whisper и `tts-1`) из кода и зависимостей.
- [x] Обработка ошибок и таймаутов внешнего API (понятное сообщение пользователю, лог).
- [x] Корректная работа с аудио-форматами (вход `.webm` из браузера, выход — поток аудио).

**Критерий приёмки:** полный голосовой цикл (голос → текст → ответ → голос) работает только на ElevenLabs + OpenAI-чат.

---

## Фаза 6 — Логирование и наблюдаемость

> Цель: понимать, что происходит, и быстро находить проблемы.

- [x] Структурированное логирование (уровни, JSON-формат), вывод в stdout (для Docker).
- [x] Request-id на каждый запрос, сквозной через логи.
- [x] Логировать обращения к внешним API: длительность, статус, ошибки (без утечки ключей и персональных данных).
- [x] Глобальный обработчик ошибок: единый формат ответа об ошибке + лог стектрейса.
- [x] Healthcheck-эндпоинт с проверкой доступности БД.

**Критерий приёмки:** по логам можно проследить один запрос целиком; секреты в логи не попадают.

---

## Фаза 7 — Защита от нагрузок и взлома

> Цель: «нормальный минимальный продукт», а не открытый прокси к платным API.

- [x] **Rate limiting** на тяжёлые/платные эндпоинты (`/stt`, `/chat`, `/tts`, `/auth/login`) — по пользователю и по IP.
- [x] **Валидация входных данных:** лимит размера загружаемого аудио, максимальная длина текста для TTS, проверка типов/форматов (Pydantic-схемы).
- [x] **Ограничение размера запроса** на уровне сервера и nginx.
- [x] **CORS**: разрешить только нужные домены (сейчас открыт для всех).
- [x] **Секреты только в переменных окружения**, ничего в коде/репозитории.
- [x] **Заголовки безопасности** и **HTTPS** в nginx (security headers; TLS для прод/внешнего доступа — микрофон требует защищённый контекст).
- [x] Брутфорс-защита на логине (rate limit + задержки), безопасные сообщения об ошибках (не раскрывать, существует ли пользователь).
- [x] (Опционально) лимит расходов на пользователя/в сутки на платные API.

**Критерий приёмки:** превышение лимитов даёт 429; некорректные/слишком большие запросы отклоняются; эндпоинты закрыты авторизацией; CORS ограничен.

---

## Фаза 8 — Фронтенд (ведётся параллельно с Фазы 4)

> Цель: UI под новый функционал, сама сфера остаётся.

- [x] Экран входа/регистрации, хранение JWT, добавление токена в запросы.
- [x] Боковая панель со списком чатов: создать, выбрать, переименовать, удалить.
- [x] Переключение чата подгружает его историю.
- [x] Сохранить существующую анимацию сферы и состояния (`idle/recording/thinking/speaking`).
- [x] Обновить URL/обработку ошибок под новые ответы бэкенда.

**Критерий приёмки:** пользователь логинится, ведёт несколько чатов голосом, переключается между ними.

---

## Фаза 9 — Тесты, документация, финальная сборка

- [x] Базовые тесты: авторизация, CRUD чатов, мок внешних API для `/chat`, `/stt`, `/tts`.
- [x] Обновить корневой `README.md` под новую архитектуру и запуск.
- [x] Финальный `docker-compose.yml`: backend + postgres + frontend(nginx), переменные из `.env`, healthcheck'и, `depends_on`.
- [ ] Smoke-проверка полного цикла на чистом окружении (`docker compose up` с нуля).

**Критерий приёмки:** проект поднимается с нуля одной командой и проходит полный голосовой сценарий.

---

## Что НЕ входит в MVP (чтобы не растекаться)

- Стриминг ответов ChatGPT/аудио в реальном времени.
- Соцсети/OAuth-вход, восстановление пароля по почте.
- Админка, аналитика, биллинг.
- Горизонтальное масштабирование, очереди, кэш (Redis) — только если упрёмся в нагрузку.

## Решения, которые стажёры НЕ принимают сами (согласовать)

- Конкретная модель ChatGPT и системный промпт.
- `voice_id` и модель ElevenLabs.
- Лимиты rate limiting и размера запросов.
- Сколько сообщений помнить при передаче в ChatGPT (срок хранения уже зафиксирован — 72 ч).
